{
  "name": "server-template",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon ./src/index.ts",
    "build": "swc src -d dist --source-maps && zip -r dist/api.zip dist/ node_modules/",
    "manual-deploy": "chmod +x scripts/manual-deploy.sh && scripts/manual-deploy.sh",
    "init-db": "chmod +x scripts/init-db.sh && scripts/init-db.sh",
    "migration-create": "chmod +x scripts/create-migration.sh && ./scripts/create-migration.sh",
    "migration-run": "yarn ts-node scripts/migration.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@vendia/serverless-express": "^4.12.6",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "pg": "^8.13.1",
    "reflect-metadata": "^0.2.2",
    "tsconfig-paths": "^4.2.0",
    "typeorm": "^0.3.20"
  },
  "devDependencies": {
    "@swc/cli": "^0.5.1",
    "@swc/core": "^1.9.3",
    "@types/express": "^5.0.0",
    "nodemon": "^3.1.7",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
